name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  typecheck-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"
          cache-dependency-path: pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @sis/shared build
      - run: pnpm --filter @sis/api exec prisma generate
      - name: Run API unit tests
        run: pnpm --filter @sis/api test --ci
      - run: pnpm --filter @sis/web exec tsc --noEmit
      - run: pnpm --filter @sis/api build
      - name: Run readiness checks (static only)
        continue-on-error: true
        run: bash scripts/readiness-check.sh
